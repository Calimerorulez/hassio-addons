# Security

This app only proxies requests received through Home Assistant Ingress to the configured Node-RED backend.

Do not expose its internal Ingress port directly. Keep Home Assistant, Node-RED and this app updated.

Security issues may be reported privately through GitHub's security reporting features when available.
